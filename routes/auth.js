const router = require("express").Router();
const berypt = require("beryptjs");

const jwt = require("jsonwebtoken");

const User = require("../model/user");

//Signup users


module.exports = router;